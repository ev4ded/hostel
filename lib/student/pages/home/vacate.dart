import 'package:flutter/material.dart';

class Vacate extends StatelessWidget {
  const Vacate({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(child: Text("Vaccate")),
    );
  }
}
